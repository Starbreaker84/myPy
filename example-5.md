### 1. Markdown
Как видно по репозиторию, я уже давно сам оформляю большинство документации именно в md формате. Де-факто это уже один из стандартов разработки.

### 2. Python setup
В качестве ОС я использую Windows 11, с настроенным WSL2, где как раз и хранятся проектные каталоги, установлен Python, использую env и uv.
В качестве редактора к сожалению лучше PyCharm пока не нашёл(раассматривал нашу OpenIDE, но она даже корректно работать с WSL не смогла).
Также ориентуруюсь на переход на LazyVIM - фактически преднастроенный Vim c донастройкой под нужный стэк, IDE раотающая со скоростью молнии.
